#include <stdio.h>
#include "stack.h"

int main()
{
    stack_t my_stack;
    init(&my_stack);

    /*push(&my_stack, 10);
    push(&my_stack, 12);
    int top = get_top(&my_stack);
    printf("top element: %d\n", top);
    pop(&my_stack);
    printf("top element: %d\n", get_top(&my_stack));*/



    printf("Hello, World!\n");
    return 0;
}